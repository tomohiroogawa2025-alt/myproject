name: Update amakara RSS

on:
  push:
    branches: [main]
    paths:
      - "amakaratecho-rss/generate_feed.py"
      - "amakaratecho-rss/requirements.txt"
      - ".github/workflows/amakaratecho-rss.yml"
  schedule:
    # Once a day at 22:27 UTC = 07:27 JST
    - cron: "27 22 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: amakaratecho-rss
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: amakaratecho-rss/requirements.txt

      - name: Install dependencies
        run: pip install -r amakaratecho-rss/requirements.txt

      - name: Generate feed
        run: python amakaratecho-rss/generate_feed.py

      - name: Commit feed when changed
        run: |
          if git diff --quiet -- amakaratecho-rss/feed.xml; then
            echo "No feed changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add amakaratecho-rss/feed.xml
          git commit -m "Update amakara RSS"
          git push
