name: Update NiEW RSS

on:
  push:
    branches: [main]
    paths:
      - "niew-rss/generate_feed.py"
      - "niew-rss/requirements.txt"
      - ".github/workflows/niew-rss.yml"
  schedule:
    # Once a day at 22:37 UTC = 07:37 JST
    - cron: "37 22 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: niew-rss
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: niew-rss/requirements.txt

      - name: Install dependencies
        run: pip install -r niew-rss/requirements.txt

      - name: Generate feed
        run: python niew-rss/generate_feed.py

      - name: Commit feed when changed
        run: |
          if git diff --quiet -- niew-rss/feed.xml; then
            echo "No feed changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add niew-rss/feed.xml
          git commit -m "Update NiEW RSS"
          git push
